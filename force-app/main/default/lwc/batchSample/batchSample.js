import { LightningElement } from 'lwc';
// import { executeBatch } from '@salesforce/apex/batchController.executeBatch';

export default class BatchSample extends LightningElement {

    handleClick(event) {
        //executeBatch();
    }
}